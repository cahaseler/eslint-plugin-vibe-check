// This is a sample file to test the max-file-lines rule
// This file has exactly 401 lines, which should trigger the default max of 400

'use strict';

// The rest of the file is just filler content to create 401 lines
function exampleFunction() {
  console.log('This is an example function');
}

const data = [
  { id: 1, name: 'Item 1' },
  { id: 2, name: 'Item 2' },
  { id: 3, name: 'Item 3' },
];

class ExampleClass {
  constructor() {
    this.value = 0;
  }

  increment() {
    this.value++;
  }

  decrement() {
    this.value--;
  }

  getValue() {
    return this.value;
  }
}

// Generate a lot of lines to hit our 301 line count
const generateLines = (count) => {
  for (let i = 0; i < count; i++) {
    // Each line below adds to our total
    console.log(`Line ${i + 1}`);
  }
};

// Calling with a large number to generate many lines - note this doesn't actually create lines in the file,
// it would only log them when the code runs. For our ESLint test, we need to add actual lines to the file.

// Adding several hundred empty lines below to exceed 400 lines total:








































































































































































































































































































































































































































































































































































